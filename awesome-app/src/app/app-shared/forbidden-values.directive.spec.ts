import { ForbiddenValuesDirective } from './forbidden-values.directive';

describe('ForbiddenValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenValuesDirective();
    expect(directive).toBeTruthy();
  });
});
